# WeatherAwake

To get weather data, you\`ll need an API key from openweather (https://openweathermap.org/).

Then create a new file called `.env` and include in that file `VITE_API_KEY=YOUR_KEY_HERE`, or
paste the API key in place of `const apiKey = import.meta.env.VITE_API_KEY;` on index.js.
